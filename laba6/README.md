# 🧩 Лабораторная работа: Работа с файловой системой Linux

## **1. Создание структуры каталогов**

Создаем иерархию подкаталогов в домашней директории:

```bash
cd ~
mkdir -p ~/Nekrasov/{1,4}
mkdir -p ~/Nekrasov/1/{2,3}
```

**Итоговая структура:**

```
~/Nekrasov
├── 1
│   ├── 2
│   └── 3
└── 4
```

---

## **2. Копирование файла `/etc/group`**

Копируем файл в каждый из созданных каталогов, используя **абсолютные пути**:

```bash
cp /etc/group ~/Nekrasov/1/
cp /etc/group ~/Nekrasov/1/2/
cp /etc/group ~/Nekrasov/1/3/
cp /etc/group ~/Nekrasov/4/
```

Проверка содержимого каталога:

```bash
ls -la ~/Nekrasov/1/2/
```

**Результат:**

```
total 12
drwxr-xr-x 2 vadooos_s vadooos_s 4096 Nov 10 12:02 .
drwxr-xr-x 4 vadooos_s vadooos_s 4096 Nov 10 12:02 ..
-rw-r--r-- 1 vadooos_s vadooos_s  834 Nov 10 12:02 group
```

---

## **3. Проверка типов файлов с помощью `file`**

```bash
file /etc/passwd
file /bin/ls
file /dev/null
file ~/Nekrasov/1/group
```

**Вывод:**

```
/etc/passwd: ASCII text
/bin/ls: ELF 64-bit LSB pie executable, dynamically linked
/dev/null: character special (1/3)
/home/vadooos_s/Nekrasov/1/group: ASCII text
```

---

## **4. Просмотр содержимого каталога `/dev`**

```bash
ls -l /dev
```

Тип файла определяется по первому символу строки:

| Символ | Тип файла             | Пример                     |
| :----- | :-------------------- | :------------------------- |
| `d`    | Каталог               | `/dev/pts`                 |
| `l`    | Символьная ссылка     | `/dev/fd -> /proc/self/fd` |
| `s`    | Сокет                 | `/dev/log`                 |
| `b`    | Блочное устройство    | `/dev/sda`                 |
| `c`    | Символьное устройство | `/dev/null`                |
| `p`    | Именованный канал     | `/dev/initctl`             |

---

## **5. Ключи утилиты `ls`**

| Ключ      | Назначение                      |
| --------- | ------------------------------- |
| `-R`      | Рекурсивный вывод подкаталогов  |
| `-1`      | Один файл на строку             |
| `-m`      | Список через запятую            |
| `--color` | Цветное отображение типов       |
| `-t`      | Сортировка по времени изменения |
| `-S`      | Сортировка по размеру           |
| `-X`      | Сортировка по расширению        |
| `-r`      | Обратный порядок                |

---

## **6. Создание ссылок**

Создаем **жесткую** и **символьную** ссылки на файл `group`:

```bash
ln ~/Nekrasov/1/group ~/Nekrasov/1/group_hardlink
ln -s ~/Nekrasov/1/group ~/Nekrasov/1/group_symlink
```

Проверка:

```bash
ls -l ~/Nekrasov/1/
```

**Результат:**

```
-rw-r--r-- 1 vadooos_s vadooos_s  834 Nov 10 12:02 group
-rw-r--r-- 1 vadooos_s vadooos_s  834 Nov 10 12:05 group_hardlink
lrwxrwxrwx 1 vadooos_s vadooos_s   20 Nov 10 12:05 group_symlink -> /home/vadooos_s/Nekrasov/1/group
```

---

## **7. Индивидуальные задания**

### 🔹 Задание для бригады №4

**1. Вывести список файлов из `/tmp`, отсортированный по имени:**

```bash
ls -l /tmp | sort -k9
```

**Вывод:**

```
total 24
-rw-r--r-- 1 vadooos_s vadooos_s   73 Oct 25 18:14 remote-wsl-loc.txt
drwx------ 2 root      root      4096 Oct 22 17:40 snap-private-tmp
drwx------ 3 root      root      4096 Oct 22 17:40 systemd-private-...
-rw------- 1 vadooos_s vadooos_s 3479 Oct 25 18:14 vscode-distro-env.SZElnU
```

**2. Найти файлы, имена которых оканчиваются на `text`:**

```bash
find /tmp -type f -name "*text"
```

**Результат:**

```
find: ‘/tmp/systemd-private-...’: Permission denied
find: ‘/tmp/snap-private-tmp’: Permission denied
```

*(нет доступных файлов, удовлетворяющих условию)*

---


